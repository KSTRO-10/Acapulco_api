---
description: Automatically stage, commit, and push changes to git
---

When completing a task or a significant set of changes:
1. Stage all changes
// turbo
2. `git add .`
3. Commit with a descriptive message
// turbo
4. `git commit -m "..."`
5. Push to the remote repository
// turbo
6. `git push origin main`
